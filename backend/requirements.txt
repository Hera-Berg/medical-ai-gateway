# Backend dependencies. Pinned minor versions.
# ML/RAG deps (langchain, sentence-transformers, qdrant-client, boto3,
# umap-learn) are added in their respective build steps to keep the image lean
# until needed.
fastapi==0.111.0
uvicorn[standard]==0.30.1
httpx==0.27.0
pydantic==2.7.4
pydantic-settings==2.3.4
sqlalchemy[asyncio]==2.0.31
asyncpg==0.29.0
alembic==1.13.2
greenlet==3.0.3
anyio==4.4.0

# ── Step 5: RAG pipeline ──
pypdfium2==4.30.0
langchain-text-splitters==0.2.2
fastembed==0.3.6
qdrant-client==1.10.1
python-multipart==0.0.9

# ── Step 8: RAG Inspector (dimensionality reduction) ──
scikit-learn==1.5.1
numpy==1.26.4

# ── Step 9: AWS storage backend ──
boto3==1.34.144

# ── Step 11: inference (model config) ──
pyyaml==6.0.1
