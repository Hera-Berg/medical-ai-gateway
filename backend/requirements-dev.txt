# Dev/test-only dependencies (NOT installed into the runtime image).
# moto fakes AWS services so the AWS storage backend logic can be tested
# without real credentials or billable resources.
-r requirements.txt
moto[s3]==5.0.11
pytest==8.2.2
pytest-asyncio==0.23.8
